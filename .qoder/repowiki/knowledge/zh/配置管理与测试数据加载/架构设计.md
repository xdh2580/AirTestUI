- 核心逻辑位于 `config/settings.py`，通过 `_load_yaml` 加载 `config.yaml`。
- 支持通过 `AIRTESTUI_CONFIG_PATH` 指定配置文件路径，并自动合并 `local_config.yaml`（若存在）。
- 提供 `_Config` 类封装配置数据，支持属性和字典方式访问。
- `data/test_data.yaml` 存储测试用例数据，供测试框架加载使用。