- 采用分层架构设计，严格区分基础驱动 (base)、页面对象 (pages)、业务用例 (testcases) 与通用工具 (utils)。
- 实施平台隔离策略，通过目录结构 (android/ios) 与 pytest markers 实现 Android 与 iOS 用例的物理及逻辑分离。
- 统一使用 YAML 文件 (config.yaml, test_data.yaml) 管理环境配置与测试数据，并通过 settings 模块进行集中加载。