- 所有公共操作方法必须使用 `@allure.step` 装饰器以生成自动化测试报告步骤。
- 页面类初始化时，若涉及图像识别，需在 `__init__` 中通过 `self.resource_path` 预加载 `Template` 对象。
- 元素交互方法需遵循“Poco 优先，图像兜底”的逻辑结构，即在 `if self._poco:` 分支处理 UI 树定位，`else` 分支处理图像定位。