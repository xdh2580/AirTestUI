- 测试类命名遵循 `Test[Platform][Feature]` 格式（如 `TestAndroidLogin`），并继承自 `object` 或无显式基类。
- 每个测试类均包含 `setup` fixture，用于在测试开始前实例化对应的页面对象并传入 `poco` 实例。
- 所有测试方法均使用 `@allure.title` 描述用例标题，并使用 `@allure.severity` 标注严重程度。
- 断言逻辑统一使用 `assert` 语句，并在失败时提供明确的中文错误提示信息。